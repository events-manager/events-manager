{
  "name": "@spider/conferences",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "dev": "npm-run-all -p dev:api dev:dashboard",
    "install": "npm-run-all -p install:api install:dashboard",
    "build": "npm-run-all -p build:api build:dashboard && npm run deploy",
    "dev:api": "cd ./api && npm run dev",
    "dev:dashboard": "cd ./dashboard && npm run dev",
    "install:api": "cd ./api && npm i",
    "install:dashboard": "cd ./dashboard && npm i",
    "build:api": "cd api && npm run build",
    "build:dashboard": "cd dashboard && npm run build",
    "deploy": "sh ./deploy.sh",
    "nukeit": "rm -rf node_modules npm.lock && rm -rf ./dashboard/node_modules ./dashboard/npm.lock && rm -rf ./api/node_modules ./api/npm.lock"
  },
  "author": "Chris <info@cloudspider.io>",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
